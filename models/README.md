# Models

Store serialized ML/DL artifacts in this directory. Keep both the latest production
build and any intermediate exports required for reproducibility.

Current contents:
- `trained_model.h5`
- `trained_model.keras`
